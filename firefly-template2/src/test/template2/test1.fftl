#extends hello.common.Test;

/*
main function
*/
#main
    // call function
    #include(this.hello("my first function"))

    // control flow
    #set(count = 0)
    #while (${count} < 20)
        #if (${count} % 3 == 0)
            ``red``
        #else if (${count} % 3 == 1)
            ``blue``
        #else
            ``black``
        #end
        #set(count = ${count} + 1)
    #end

    #for (book : ${bookList})
    ```book name: ``` ${book.name} &nl;
    ```author: ``` ${book.author} &nl;
    ```introduce: ``` ${book.content[0]["introduce"].substring(0, ${maxSize})} &nl;
    #end

    #switch (${brand})
        #case "apple"
        ``cool macbook pro``
        #break

        #case "sony"
        ``black technology``
        #break

        #case "pentax"
        ``solid & reliable``
        #break

        #default
        ``"nothing" wrong``
        #break
    #end

    ```
    test
    &oquot;string&oquot;
    ```
#end

// show hello message
#function hello(message)
    ```hello``` ${message}
#end