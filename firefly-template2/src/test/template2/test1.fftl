#extends hello.common.Test;

#main
    #while (${a} = 3 + --4.4++ * 5 > ${sdfsdf.ss[22]})
        ```dfsdf```${sfsf}
        #if (${x} == true)
        #end
    #end
    #include(common.Foo.get(3))
    #include(this.hello())

    ${ssfdsf[33]}

    #for (ss : ${ssssdf})
    #end

    #switch (${x})
        #case "ssdfdsf"
        #break

        #case "ss"
        #break

        #case "hello"
        #break

        #default
        #break
    #end
    ```test &oquot;&oquot;```
    ${str.length()}
    ${test.hello1("test1", "test2")}
#end

#function hello1(arg1, arg2)
    ```testaaa```${arg1}
    ```dsfdsf```${arg2}
    #if (${arg1.length()} > 3)
        ```ok```
    #end

    #include(test2.go)
#end