#extends common.Layout;

/*
main function
*/
#main
    // call function
    #include(super.main())
    #include(this.hello("my first function"))

    // control flow
    #set(count = 0)
    #while (${count} < 20)
        #if (${count} % 3 == 0)
            ``red``&nl;
        #else if (${count} % 3 == 1)
            ``blue``&nl;
        #else
            ``black``&nl;
        #end
        #set(count = ${count} + 1)
    #end

    #for (book : ${bookList})
    ``book name: ${book.name}`` &nl;
    ``author: ${book.author}`` &nl;
    ``introduce: ${book.content[0]["introduce"].substring(0, ${maxSize})}`` &nl;
    #end

    #switch (${brand})
        #case "apple"
        ``cool macbook pro``&nl;
        #break

        #case "sony"
        ``black technology``&nl;
        #break

        #case "pentax"
        ``solid & reliable``&nl;
        #break

        #default
        ``"nothing" wrong``&nl;
        #break
    #end

    ```
    test
    &oquot;string&oquot;
    ${escape}
    ```
#end

// show hello message
#function hello(message)
    ``hello  ${message}``
#end